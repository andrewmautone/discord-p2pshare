node_modules/
*.log

# Saída de build. O .plugin.js é a exceção: o auto-update busca ele por raw,
# então precisa estar versionado. O instalador (2 MB) vai para Releases.
release/*
!release/P2PShare.plugin.js

# Artefatos do Rust
native/audio-helper/target/
