# P2PShare Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Plugin Vencord que transmite a tela para os membros de um canal de voz via WebRTC ponto-a-ponto, usando mensagens do próprio canal como canal de sinalização e sem nenhum servidor próprio.

**Architecture:** Topologia mesh — o broadcaster mantém uma `RTCPeerConnection` por viewer, todas enviando a mesma `MediaStreamTrack`. A sinalização usa duas mensagens: um *beacon* legível por humanos (com o `sessionId` escondido em caracteres zero-width) e *handshakes* offer/answer enviados sempre como anexo `.txt`, roteados pelo nome do arquivo e auto-deletados. ICE é não-trickle: cada lado espera o gathering completar e posta um SDP único.

**Tech Stack:** TypeScript, React, Vencord plugin API (`definePlugin`, `definePluginSettings`, `findByPropsLazy`), WebRTC (`RTCPeerConnection`), API do Discord via webpack (`MessageActions`, `CloudUpload`, `RestAPI`, `FluxDispatcher`). Testes com o test runner nativo do Node (`node:test`) rodado através do `tsx`, que já é devDependency do Vencord — nenhuma dependência nova é adicionada.

**Spec:** `docs/superpowers/specs/2026-08-18-p2pshare-design.md`

## Global Constraints

- **Zero dependências novas.** Nada de `pnpm add`. Tudo que o plugin usa já existe no Vencord ou no runtime do Chromium.
- **Diretório de trabalho de todos os comandos:** `C:\Users\Andrew\Vencord` (o repo do Vencord). O plugin vive em `src/userplugins/p2pShare/`, que é um repo git próprio — **todos os commits são feitos de dentro de `src/userplugins/p2pShare`**.
- **Autoria dos commits:** `git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit`.
- **Caminhos neste plano são relativos a `src/userplugins/p2pShare/`** salvo quando começam com `src/`.
- **Módulos puros** (`codec.ts`, `bitrate.ts`) não podem importar nada do Vencord nem tocar em globais do browser. É isso que os torna testáveis com `node:test`.
- **Servidores STUN** (valores exatos): `stun:stun.l.google.com:19302` e `stun:stun.cloudflare.com:3478`.
- **Alfabeto zero-width** (valores exatos): dígitos `\u200b`, `\u200c`, `\u200d`, `\u2060`; delimitador `\u2062`.
- **Formato do nome de handshake** (exato): `p2p.<sessionId>.<kind>.<targetUserId>.txt`, `kind ∈ {offer, answer}`.
- **Limites de bitrate** (exatos): mínimo 800000 bps, máximo 8000000 bps, orçamento padrão 15 Mbps.
- **Timeouts** (exatos): ICE gathering 4000 ms; conexão do peer 30000 ms; auto-delete de handshake 20000 ms.
- **Versão do protocolo:** `v: 1` em todo payload JSON.
- **Licença:** o Vencord é GPL-3.0-or-later. Todo arquivo `.ts`/`.tsx` novo começa com o header de licença de 4 linhas usado pelos plugins existentes (ver `src/plugins/voiceMessages/index.tsx`).

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `codec.ts` | Puro. Encode/decode zero-width, embed/extract de payload, format/parse do nome de handshake, geração de `sessionId`. |
| `bitrate.ts` | Puro. Orçamento de upload → `maxBitrate` por peer. |
| `constants.ts` | Puro. STUN, timeouts, limites, versão do protocolo, URL do repo. |
| `discord/api.ts` | Wrappers finos sobre `MessageActions`, `CloudUpload`, `RestAPI`. Único ponto que conhece o formato das chamadas do Discord. |
| `discord/events.ts` | Assinatura de `MESSAGE_CREATE` / `MESSAGE_DELETE` via `FluxDispatcher`. |
| `capture.ts` | Cadeia de fallback para obter o `MediaStream` da tela. |
| `peers.ts` | Ciclo de vida das `RTCPeerConnection`. `BroadcastPeers` (emissor) e `ViewerPeer` (receptor). |
| `signaling.ts` | Cola: publica/observa/deleta beacon e handshakes, traduzindo mensagem do Discord ↔ evento de domínio. |
| `broadcast.ts` | Orquestra o lado emissor (captura + beacon + peers). |
| `watch.ts` | Orquestra o lado receptor (descoberta + offer + stream). |
| `ui/ViewerOverlay.tsx` | Overlay flutuante arrastável/redimensionável. |
| `ui/BeaconAccessory.tsx` | Botão "Assistir" renderizado no beacon. |
| `index.tsx` | `definePlugin`, settings, comando slash, wiring. |
| `README.md` | Instalação e uso. |

Ordem das tasks: módulos puros primeiro (testáveis de verdade), depois as camadas que dependem do Discord, depois a UI.

---

### Task 1: Scaffold e módulo `constants.ts`

**Files:**
- Create: `constants.ts`
- Create: `.gitignore`
- Create: `README.md`

**Interfaces:**
- Consumes: nada
- Produces: `ICE_SERVERS: RTCIceServer[]`, `ZW_DIGITS: readonly string[]`, `ZW_DELIM: string`, `PROTOCOL_VERSION: number`, `MIN_BITRATE: number`, `MAX_BITRATE: number`, `DEFAULT_BUDGET_MBPS: number`, `ICE_GATHER_TIMEOUT_MS: number`, `PEER_CONNECT_TIMEOUT_MS: number`, `HANDSHAKE_TTL_MS: number`, `PLUGIN_URL: string`

- [ ] **Step 1: Criar `.gitignore`**

```
node_modules/
*.log
```

- [ ] **Step 2: Criar `constants.ts`**

```ts
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

export const PROTOCOL_VERSION = 1;

export const PLUGIN_URL = "https://github.com/andrewmautone/discord-p2pshare";

export const ICE_SERVERS: RTCIceServer[] = [
    { urls: "stun:stun.l.google.com:19302" },
    { urls: "stun:stun.cloudflare.com:3478" }
];

/** Quatro code points invisíveis, 2 bits por caractere. */
export const ZW_DIGITS = ["\u200b", "\u200c", "\u200d", "\u2060"] as const;
/** Delimita o payload zero-width dentro do texto visível. */
export const ZW_DELIM = "\u2062";

export const MIN_BITRATE = 800_000;
export const MAX_BITRATE = 8_000_000;
export const DEFAULT_BUDGET_MBPS = 15;

export const ICE_GATHER_TIMEOUT_MS = 4_000;
export const PEER_CONNECT_TIMEOUT_MS = 30_000;
export const HANDSHAKE_TTL_MS = 20_000;
```

- [ ] **Step 3: Criar `README.md`**

```markdown
# P2PShare

Compartilhamento de tela ponto-a-ponto para Discord, via Vencord.

O vídeo vai direto de PC para PC por WebRTC — sem passar pela infra de vídeo
do Discord e sem cap de bitrate. A sinalização usa mensagens do próprio canal
de voz, então não existe servidor.

## Instalação

```bash
git clone https://github.com/Vendicated/Vencord
cd Vencord
pnpm i
git clone <url deste repo> src/userplugins/p2pShare
pnpm build
pnpm inject
```

Ative **P2PShare** nas configurações do Vencord.

## Uso

- **Transmitir:** entre num canal de voz e use o botão P2P no painel de voz,
  ou o comando `/p2pshare`.
- **Assistir:** quando alguém transmite, aparece um botão **Assistir** na
  mensagem de aviso no chat do canal.

## Limitações

- Sem TURN. Quem estiver atrás de NAT simétrico (CGNAT, algumas redes
  corporativas) não conecta.
- Mesh: o upload do broadcaster é dividido entre os viewers. Acima de ~6
  pessoas a qualidade fica ruim.
- Client mods violam os Termos de Serviço do Discord.
```

- [ ] **Step 4: Verificar que o TypeScript aceita os arquivos**

Run: `pnpm exec tsc --noEmit`
Expected: PASS (sem erros mencionando `p2pShare`)

- [ ] **Step 5: Commit**

```bash
cd src/userplugins/p2pShare
git add .gitignore constants.ts README.md
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: constantes do protocolo e scaffold"
```

---

### Task 2: `codec.ts` — zero-width e nomes de handshake

Todo o formato de fio do plugin vive aqui, sem nenhuma dependência. É a peça mais fácil de errar e a mais barata de testar.

**Files:**
- Create: `codec.ts`
- Test: `codec.test.ts`

**Interfaces:**
- Consumes: `constants.ts` (`ZW_DIGITS`, `ZW_DELIM`, `PROTOCOL_VERSION`)
- Produces:
  - `encodeZeroWidth(text: string): string`
  - `decodeZeroWidth(zw: string): string`
  - `embedPayload(visible: string, payload: unknown): string`
  - `extractPayload<T>(content: string): T | null`
  - `type HandshakeKind = "offer" | "answer"`
  - `interface HandshakeName { sessionId: string; kind: HandshakeKind; targetUserId: string; }`
  - `formatHandshakeName(n: HandshakeName): string`
  - `parseHandshakeName(filename: string): HandshakeName | null`
  - `newSessionId(): string`

- [ ] **Step 1: Escrever os testes que falham**

Create `codec.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, it } from "node:test";

import {
    decodeZeroWidth,
    embedPayload,
    encodeZeroWidth,
    extractPayload,
    formatHandshakeName,
    newSessionId,
    parseHandshakeName
} from "./codec";

describe("zero-width codec", () => {
    it("faz roundtrip de ascii", () => {
        assert.equal(decodeZeroWidth(encodeZeroWidth("hello")), "hello");
    });

    it("faz roundtrip de acentos e emoji", () => {
        const s = "ação 🔴 ünïcode";
        assert.equal(decodeZeroWidth(encodeZeroWidth(s)), s);
    });

    it("faz roundtrip de string vazia", () => {
        assert.equal(decodeZeroWidth(encodeZeroWidth("")), "");
    });

    it("produz apenas caracteres invisíveis", () => {
        const out = encodeZeroWidth("abc");
        assert.match(out, /^[\u200b\u200c\u200d\u2060]+$/);
    });

    it("usa 4 caracteres por byte", () => {
        // "abc" = 3 bytes UTF-8
        assert.equal(encodeZeroWidth("abc").length, 12);
    });
});

describe("payload embutido", () => {
    it("preserva o texto visível", () => {
        const msg = embedPayload("🔴 transmitindo", { v: 1, s: "abc12345" });
        assert.ok(msg.startsWith("🔴 transmitindo"));
    });

    it("recupera o payload", () => {
        const msg = embedPayload("oi", { v: 1, s: "abc12345" });
        assert.deepEqual(extractPayload(msg), { v: 1, s: "abc12345" });
    });

    it("devolve null quando não há payload", () => {
        assert.equal(extractPayload("mensagem normal"), null);
    });

    it("devolve null quando o payload é JSON inválido", () => {
        const broken = "oi\u2062" + encodeZeroWidth("{nao é json") + "\u2062";
        assert.equal(extractPayload(broken), null);
    });

    it("devolve null quando o delimitador não fecha", () => {
        assert.equal(extractPayload("oi\u2062" + encodeZeroWidth("{}")), null);
    });
});

describe("nome de handshake", () => {
    it("faz roundtrip", () => {
        const n = { sessionId: "abc12345", kind: "offer" as const, targetUserId: "123456789" };
        assert.deepEqual(parseHandshakeName(formatHandshakeName(n)), n);
    });

    it("formata no padrão esperado", () => {
        const name = formatHandshakeName({ sessionId: "abc12345", kind: "answer", targetUserId: "42" });
        assert.equal(name, "p2p.abc12345.answer.42.txt");
    });

    it("rejeita nome de outro arquivo", () => {
        assert.equal(parseHandshakeName("foto.png"), null);
    });

    it("rejeita kind desconhecido", () => {
        assert.equal(parseHandshakeName("p2p.abc12345.candidate.42.txt"), null);
    });

    it("rejeita número errado de segmentos", () => {
        assert.equal(parseHandshakeName("p2p.abc12345.offer.txt"), null);
    });
});

describe("newSessionId", () => {
    it("tem 8 caracteres alfanuméricos", () => {
        assert.match(newSessionId(), /^[a-z0-9]{8}$/);
    });

    it("não repete em 100 chamadas", () => {
        const ids = new Set(Array.from({ length: 100 }, () => newSessionId()));
        assert.equal(ids.size, 100);
    });
});
```

- [ ] **Step 2: Rodar para confirmar que falha**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/codec.test.ts`
Expected: FAIL — `Cannot find module './codec'`

- [ ] **Step 3: Implementar `codec.ts`**

```ts
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import { ZW_DELIM, ZW_DIGITS } from "./constants";

const DIGIT_INDEX = new Map(ZW_DIGITS.map((c, i) => [c, i]));

/** Codifica texto em caracteres invisíveis: 1 byte UTF-8 → 4 caracteres. */
export function encodeZeroWidth(text: string): string {
    const bytes = new TextEncoder().encode(text);
    let out = "";
    for (const byte of bytes) {
        out += ZW_DIGITS[(byte >> 6) & 3];
        out += ZW_DIGITS[(byte >> 4) & 3];
        out += ZW_DIGITS[(byte >> 2) & 3];
        out += ZW_DIGITS[byte & 3];
    }
    return out;
}

/** Inverso de encodeZeroWidth. Caracteres desconhecidos abortam com erro. */
export function decodeZeroWidth(zw: string): string {
    const chars = [...zw];
    if (chars.length % 4 !== 0) throw new Error("comprimento zero-width inválido");

    const bytes = new Uint8Array(chars.length / 4);
    for (let i = 0; i < bytes.length; i++) {
        let byte = 0;
        for (let j = 0; j < 4; j++) {
            const digit = DIGIT_INDEX.get(chars[i * 4 + j]);
            if (digit === undefined) throw new Error("caractere zero-width inválido");
            byte = (byte << 2) | digit;
        }
        bytes[i] = byte;
    }
    return new TextDecoder().decode(bytes);
}

/** Anexa um payload JSON invisível ao fim de um texto legível. */
export function embedPayload(visible: string, payload: unknown): string {
    return visible + ZW_DELIM + encodeZeroWidth(JSON.stringify(payload)) + ZW_DELIM;
}

/** Extrai o payload embutido. Devolve null se não houver ou estiver corrompido. */
export function extractPayload<T>(content: string): T | null {
    const start = content.indexOf(ZW_DELIM);
    if (start === -1) return null;
    const end = content.indexOf(ZW_DELIM, start + 1);
    if (end === -1) return null;

    try {
        return JSON.parse(decodeZeroWidth(content.slice(start + 1, end))) as T;
    } catch {
        return null;
    }
}

export type HandshakeKind = "offer" | "answer";

export interface HandshakeName {
    sessionId: string;
    kind: HandshakeKind;
    targetUserId: string;
}

export function formatHandshakeName(n: HandshakeName): string {
    return `p2p.${n.sessionId}.${n.kind}.${n.targetUserId}.txt`;
}

export function parseHandshakeName(filename: string): HandshakeName | null {
    const parts = filename.split(".");
    if (parts.length !== 5) return null;

    const [prefix, sessionId, kind, targetUserId, ext] = parts;
    if (prefix !== "p2p" || ext !== "txt") return null;
    if (kind !== "offer" && kind !== "answer") return null;
    if (!sessionId || !targetUserId) return null;

    return { sessionId, kind, targetUserId };
}

export function newSessionId(): string {
    const bytes = new Uint8Array(8);
    crypto.getRandomValues(bytes);
    return [...bytes].map(b => (b % 36).toString(36)).join("");
}
```

- [ ] **Step 4: Rodar os testes**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/codec.test.ts`
Expected: PASS — 17 testes

- [ ] **Step 5: Commit**

```bash
cd src/userplugins/p2pShare
git add codec.ts codec.test.ts
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: codec zero-width e nomes de handshake"
```

---

### Task 3: `bitrate.ts` — orçamento de upload

**Files:**
- Create: `bitrate.ts`
- Test: `bitrate.test.ts`

**Interfaces:**
- Consumes: `constants.ts` (`MIN_BITRATE`, `MAX_BITRATE`)
- Produces: `computePerPeerBitrate(budgetMbps: number, viewerCount: number): number`

- [ ] **Step 1: Escrever os testes que falham**

Create `bitrate.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, it } from "node:test";

import { computePerPeerBitrate } from "./bitrate";
import { MAX_BITRATE, MIN_BITRATE } from "./constants";

describe("computePerPeerBitrate", () => {
    it("divide o orçamento entre os viewers", () => {
        // 15 Mbps / 3 = 5 Mbps
        assert.equal(computePerPeerBitrate(15, 3), 5_000_000);
    });

    it("limita no teto quando há poucos viewers", () => {
        // 15 Mbps / 1 = 15 Mbps, acima do teto
        assert.equal(computePerPeerBitrate(15, 1), MAX_BITRATE);
    });

    it("limita no piso quando há muitos viewers", () => {
        // 15 Mbps / 100 = 150 kbps, abaixo do piso
        assert.equal(computePerPeerBitrate(15, 100), MIN_BITRATE);
    });

    it("devolve o teto quando não há viewers", () => {
        assert.equal(computePerPeerBitrate(15, 0), MAX_BITRATE);
    });

    it("trata viewerCount negativo como zero", () => {
        assert.equal(computePerPeerBitrate(15, -2), MAX_BITRATE);
    });

    it("respeita orçamento pequeno", () => {
        // 4 Mbps / 2 = 2 Mbps, dentro dos limites
        assert.equal(computePerPeerBitrate(4, 2), 2_000_000);
    });
});
```

- [ ] **Step 2: Rodar para confirmar que falha**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/bitrate.test.ts`
Expected: FAIL — `Cannot find module './bitrate'`

- [ ] **Step 3: Implementar `bitrate.ts`**

```ts
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import { MAX_BITRATE, MIN_BITRATE } from "./constants";

/**
 * Divide o orçamento de upload entre os viewers conectados.
 * Sem viewers, devolve o teto — o primeiro a conectar já entra na melhor
 * qualidade e só é rebaixado quando chega companhia.
 */
export function computePerPeerBitrate(budgetMbps: number, viewerCount: number): number {
    if (viewerCount <= 0) return MAX_BITRATE;

    const perPeer = (budgetMbps * 1_000_000) / viewerCount;
    return Math.round(Math.min(MAX_BITRATE, Math.max(MIN_BITRATE, perPeer)));
}
```

- [ ] **Step 4: Rodar os testes**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/bitrate.test.ts`
Expected: PASS — 6 testes

- [ ] **Step 5: Commit**

```bash
cd src/userplugins/p2pShare
git add bitrate.ts bitrate.test.ts
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: cálculo de bitrate adaptativo"
```

---

### Task 4: Plugin carregável — `index.tsx` mínimo com settings

Antes de escrever qualquer lógica que dependa do Discord, é preciso ver o plugin aparecendo na lista do Vencord. Se este passo não funciona, nada depois dele funciona.

**Files:**
- Create: `index.tsx`

**Interfaces:**
- Consumes: `constants.ts` (`DEFAULT_BUDGET_MBPS`)
- Produces: `settings` (export nomeado) com `settings.store.uploadBudgetMbps: number`, `settings.store.overlayX/overlayY/overlayWidth: number`

- [ ] **Step 1: Escrever `index.tsx`**

```tsx
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import { definePluginSettings } from "@api/Settings";
import definePlugin, { OptionType } from "@utils/types";

import { DEFAULT_BUDGET_MBPS } from "./constants";

export const settings = definePluginSettings({
    uploadBudgetMbps: {
        type: OptionType.SLIDER,
        description: "Orçamento de upload em Mbps, dividido entre os viewers",
        markers: [5, 10, 15, 25, 50, 100],
        default: DEFAULT_BUDGET_MBPS,
        stickToMarkers: false
    },
    overlayX: { type: OptionType.NUMBER, description: "", default: 80, hidden: true },
    overlayY: { type: OptionType.NUMBER, description: "", default: 80, hidden: true },
    overlayWidth: { type: OptionType.NUMBER, description: "", default: 640, hidden: true }
});

export default definePlugin({
    name: "P2PShare",
    description: "Compartilhamento de tela ponto-a-ponto, sem passar pela infra de vídeo do Discord",
    authors: [{ name: "Andrew", id: 0n }],
    settings
});
```

- [ ] **Step 2: Buildar o Vencord**

Run: `pnpm build`
Expected: build conclui sem erro e a saída menciona `p2pShare`

- [ ] **Step 3: Verificar tipos**

Run: `pnpm exec tsc --noEmit`
Expected: PASS

- [ ] **Step 4: Verificação manual**

Rode `pnpm inject`, abra o Discord, vá em Configurações → Vencord → Plugins e confirme que **P2PShare** aparece na lista com o slider de orçamento de upload. Ative o plugin.

Se o plugin não aparece, pare e investigue antes de seguir — as tasks seguintes assumem que o carregamento funciona.

- [ ] **Step 5: Commit**

```bash
cd src/userplugins/p2pShare
git add index.tsx
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: plugin carregável com settings"
```

---

### Task 5: `discord/api.ts` e `discord/events.ts` — camada de acesso ao Discord

Todo conhecimento sobre como o Discord faz as coisas fica confinado aqui. Nenhum outro módulo importa do webpack.

**Files:**
- Create: `discord/api.ts`
- Create: `discord/events.ts`

**Interfaces:**
- Consumes: nada dos módulos anteriores
- Produces:
  - `sendMessage(channelId: string, content: string): Promise<string>` — devolve o id da mensagem
  - `deleteMessage(channelId: string, messageId: string): Promise<void>`
  - `uploadTextAttachment(channelId: string, filename: string, text: string, content: string): Promise<void>`
  - `fetchAttachmentText(url: string): Promise<string>`
  - `getCurrentUserId(): string`
  - `getVoiceChannelId(): string | null`
  - `onMessageCreate(handler: (message: DiscordMessage) => void): () => void`
  - `onMessageDelete(handler: (channelId: string, messageId: string) => void): () => void`
  - `interface DiscordMessage { id: string; channel_id: string; content: string; author: { id: string; username: string; }; attachments: { filename: string; url: string; }[]; }`

- [ ] **Step 1: Escrever `discord/api.ts`**

O upload segue o padrão de `src/plugins/voiceMessages/index.tsx:111-145` — instanciar `CloudUpload`, esperar o evento `complete`, e então postar a mensagem referenciando `uploadedFilename`.

```ts
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import { findLazy } from "@webpack";
import { Constants, RestAPI, SelectedChannelStore, SnowflakeUtils, UserStore } from "@webpack/common";
import type { CloudUpload as TCloudUpload } from "@vencord/discord-types";
import { CloudUploadPlatform } from "@vencord/discord-types/enums";

const CloudUpload: typeof TCloudUpload = findLazy(m => m.prototype?.trackUploadFinished);

export interface DiscordMessage {
    id: string;
    channel_id: string;
    content: string;
    author: { id: string; username: string; };
    attachments: { filename: string; url: string; }[];
}

export function getCurrentUserId(): string {
    return UserStore.getCurrentUser().id;
}

/** Canal de voz em que o usuário está, ou null. */
export function getVoiceChannelId(): string | null {
    return SelectedChannelStore.getVoiceChannelId() ?? null;
}

export async function sendMessage(channelId: string, content: string): Promise<string> {
    const res = await RestAPI.post({
        url: Constants.Endpoints.MESSAGES(channelId),
        body: {
            channel_id: channelId,
            content,
            nonce: SnowflakeUtils.fromTimestamp(Date.now()),
            sticker_ids: [],
            type: 0
        }
    });
    return res.body.id as string;
}

export async function deleteMessage(channelId: string, messageId: string): Promise<void> {
    await RestAPI.del({ url: Constants.Endpoints.MESSAGE(channelId, messageId) });
}

/**
 * Sobe um arquivo de texto e posta a mensagem que o referencia.
 * O corpo `content` fica com o marcador invisível para o plugin reconhecer.
 */
export function uploadTextAttachment(
    channelId: string,
    filename: string,
    text: string,
    content: string
): Promise<void> {
    return new Promise((resolve, reject) => {
        const upload = new CloudUpload({
            file: new File([text], filename, { type: "text/plain" }),
            isThumbnail: false,
            platform: CloudUploadPlatform.WEB
        }, channelId);

        upload.on("complete", () => {
            RestAPI.post({
                url: Constants.Endpoints.MESSAGES(channelId),
                body: {
                    channel_id: channelId,
                    content,
                    nonce: SnowflakeUtils.fromTimestamp(Date.now()),
                    sticker_ids: [],
                    type: 0,
                    attachments: [{
                        id: "0",
                        filename: upload.filename,
                        uploaded_filename: upload.uploadedFilename
                    }]
                }
            }).then(() => resolve(), reject);
        });
        upload.on("error", () => reject(new Error("falha ao subir anexo")));

        upload.upload();
    });
}

export async function fetchAttachmentText(url: string): Promise<string> {
    const res = await fetch(url);
    if (!res.ok) throw new Error(`anexo respondeu ${res.status}`);
    return res.text();
}
```

- [ ] **Step 2: Escrever `discord/events.ts`**

```ts
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import { FluxDispatcher } from "@webpack/common";

import type { DiscordMessage } from "./api";

/** Assina MESSAGE_CREATE. Devolve a função de cancelamento. */
export function onMessageCreate(handler: (message: DiscordMessage) => void): () => void {
    const listener = (event: { message: DiscordMessage; }) => {
        try {
            handler(event.message);
        } catch (err) {
            console.error("[P2PShare] handler de MESSAGE_CREATE falhou", err);
        }
    };

    FluxDispatcher.subscribe("MESSAGE_CREATE", listener);
    return () => FluxDispatcher.unsubscribe("MESSAGE_CREATE", listener);
}

/** Assina MESSAGE_DELETE. Devolve a função de cancelamento. */
export function onMessageDelete(handler: (channelId: string, messageId: string) => void): () => void {
    const listener = (event: { channelId: string; id: string; }) => {
        try {
            handler(event.channelId, event.id);
        } catch (err) {
            console.error("[P2PShare] handler de MESSAGE_DELETE falhou", err);
        }
    };

    FluxDispatcher.subscribe("MESSAGE_DELETE", listener);
    return () => FluxDispatcher.unsubscribe("MESSAGE_DELETE", listener);
}
```

- [ ] **Step 3: Verificar tipos e build**

Run: `pnpm exec tsc --noEmit && pnpm build`
Expected: PASS nos dois

Se `SelectedChannelStore.getVoiceChannelId` não existir no tipo, confirme o nome real em `src/webpack/common/stores.ts` e ajuste. Se `Constants.Endpoints.MESSAGE` não existir, use a string `` `/channels/${channelId}/messages/${messageId}` ``.

- [ ] **Step 4: Commit**

```bash
cd src/userplugins/p2pShare
git add discord/
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: camada de acesso ao Discord"
```

---

### Task 6: `capture.ts` — obter o stream da tela

**Files:**
- Create: `capture.ts`
- Test: `capture.test.ts`

**Interfaces:**
- Consumes: nada
- Produces:
  - `class CaptureError extends Error`
  - `captureScreen(deps?: CaptureDeps): Promise<MediaStream>`
  - `interface CaptureDeps { getDisplayMedia?: (c: any) => Promise<MediaStream>; getUserMedia?: (c: any) => Promise<MediaStream>; getSources?: () => Promise<{ id: string; name: string; }[]>; pickSource?: (s: { id: string; name: string; }[]) => Promise<string | null>; }`

O parâmetro `deps` existe para o teste — em produção ele é omitido e os defaults apontam para as APIs reais.

- [ ] **Step 1: Escrever os testes que falham**

Create `capture.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, it } from "node:test";

import { CaptureError, captureScreen } from "./capture";

const fakeStream = { id: "fake" } as unknown as MediaStream;

describe("captureScreen", () => {
    it("usa getDisplayMedia quando disponível", async () => {
        const stream = await captureScreen({
            getDisplayMedia: async () => fakeStream
        });
        assert.equal(stream, fakeStream);
    });

    it("cai para getUserMedia quando getDisplayMedia falha", async () => {
        let usedConstraints: any;
        const stream = await captureScreen({
            getDisplayMedia: async () => { throw new Error("não suportado"); },
            getSources: async () => [{ id: "screen:0", name: "Tela 1" }],
            pickSource: async sources => sources[0].id,
            getUserMedia: async c => { usedConstraints = c; return fakeStream; }
        });

        assert.equal(stream, fakeStream);
        assert.equal(usedConstraints.video.mandatory.chromeMediaSource, "desktop");
        assert.equal(usedConstraints.video.mandatory.chromeMediaSourceId, "screen:0");
    });

    it("lança CaptureError quando o usuário cancela a escolha da fonte", async () => {
        await assert.rejects(
            () => captureScreen({
                getDisplayMedia: async () => { throw new Error("não suportado"); },
                getSources: async () => [{ id: "screen:0", name: "Tela 1" }],
                pickSource: async () => null,
                getUserMedia: async () => fakeStream
            }),
            (err: Error) => err instanceof CaptureError && /cancel/i.test(err.message)
        );
    });

    it("lança CaptureError quando nenhuma API funciona", async () => {
        await assert.rejects(
            () => captureScreen({
                getDisplayMedia: async () => { throw new Error("não suportado"); },
                getSources: async () => { throw new Error("sem DiscordNative"); }
            }),
            (err: Error) => err instanceof CaptureError
        );
    });

    it("lança CaptureError quando não há nenhuma fonte", async () => {
        await assert.rejects(
            () => captureScreen({
                getDisplayMedia: async () => { throw new Error("não suportado"); },
                getSources: async () => []
            }),
            (err: Error) => err instanceof CaptureError && /fonte/i.test(err.message)
        );
    });
});
```

- [ ] **Step 2: Rodar para confirmar que falha**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/capture.test.ts`
Expected: FAIL — `Cannot find module './capture'`

- [ ] **Step 3: Implementar `capture.ts`**

```ts
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

export class CaptureError extends Error {
    constructor(message: string) {
        super(message);
        this.name = "CaptureError";
    }
}

export interface CaptureSource {
    id: string;
    name: string;
}

export interface CaptureDeps {
    getDisplayMedia?: (constraints: any) => Promise<MediaStream>;
    getUserMedia?: (constraints: any) => Promise<MediaStream>;
    getSources?: () => Promise<CaptureSource[]>;
    pickSource?: (sources: CaptureSource[]) => Promise<string | null>;
}

const defaultDeps: Required<CaptureDeps> = {
    getDisplayMedia: c => navigator.mediaDevices.getDisplayMedia(c),
    getUserMedia: c => navigator.mediaDevices.getUserMedia(c),
    getSources: async () => {
        const native = (window as any).DiscordNative?.desktopCapture;
        if (!native?.getDesktopCaptureSources) throw new Error("DiscordNative.desktopCapture indisponível");
        return native.getDesktopCaptureSources({ types: ["screen", "window"] });
    },
    // Sem picker próprio, transmite a primeira fonte (a tela principal).
    pickSource: async sources => sources[0]?.id ?? null
};

/**
 * Obtém o stream da tela tentando, em ordem:
 *   1. navigator.mediaDevices.getDisplayMedia
 *   2. DiscordNative.desktopCapture + getUserMedia com chromeMediaSource
 * A primeira que funcionar vence.
 */
export async function captureScreen(deps: CaptureDeps = {}): Promise<MediaStream> {
    const d = { ...defaultDeps, ...deps };

    try {
        return await d.getDisplayMedia({
            video: { frameRate: { ideal: 60 } },
            audio: true
        });
    } catch (err) {
        console.warn("[P2PShare] getDisplayMedia indisponível, tentando DiscordNative", err);
    }

    let sources: CaptureSource[];
    try {
        sources = await d.getSources();
    } catch (err) {
        throw new CaptureError(`nenhuma API de captura de tela disponível: ${(err as Error).message}`);
    }

    if (!sources.length) throw new CaptureError("nenhuma fonte de captura encontrada");

    const sourceId = await d.pickSource(sources);
    if (!sourceId) throw new CaptureError("captura cancelada pelo usuário");

    try {
        return await d.getUserMedia({
            audio: false,
            video: {
                mandatory: {
                    chromeMediaSource: "desktop",
                    chromeMediaSourceId: sourceId,
                    maxFrameRate: 60
                }
            }
        });
    } catch (err) {
        throw new CaptureError(`falha ao capturar a fonte: ${(err as Error).message}`);
    }
}
```

- [ ] **Step 4: Rodar os testes**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/capture.test.ts`
Expected: PASS — 5 testes

- [ ] **Step 5: Commit**

```bash
cd src/userplugins/p2pShare
git add capture.ts capture.test.ts
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: captura de tela com cadeia de fallback"
```

---

### Task 7: `peers.ts` — ciclo de vida das conexões WebRTC

O coração do plugin. Testado com uma `RTCPeerConnection` falsa injetada, para que a máquina de estados seja verificável sem browser.

**Files:**
- Create: `peers.ts`
- Test: `peers.test.ts`

**Interfaces:**
- Consumes: `constants.ts` (`ICE_SERVERS`, `ICE_GATHER_TIMEOUT_MS`, `PEER_CONNECT_TIMEOUT_MS`), `bitrate.ts` (`computePerPeerBitrate`)
- Produces:
  - `interface PeerTransport { send(kind: HandshakeKind, targetUserId: string, sdp: string): Promise<void>; }`
  - `type PeerFactory = (config: RTCConfiguration) => RTCPeerConnection`
  - `waitForIceGathering(pc: RTCPeerConnection, timeoutMs?: number): Promise<void>`
  - `class BroadcastPeers` — `constructor(stream, transport, opts: { budgetMbps: number; createPeer?: PeerFactory; })`, `handleOffer(fromUserId, sdp): Promise<void>`, `removePeer(userId): void`, `readonly viewerCount: number`, `onCountChange?: (n: number) => void`, `closeAll(): void`
  - `class ViewerPeer` — `constructor(transport, broadcasterId, opts?: { createPeer?: PeerFactory; })`, `start(): Promise<void>`, `handleAnswer(sdp): Promise<void>`, `onStream?: (s: MediaStream) => void`, `onFailed?: (reason: string) => void`, `close(): void`

- [ ] **Step 1: Escrever os testes que falham**

Create `peers.test.ts`:

```ts
import assert from "node:assert/strict";
import { describe, it } from "node:test";

import { BroadcastPeers, ViewerPeer } from "./peers";

/** RTCPeerConnection falsa, suficiente para exercitar a máquina de estados. */
class FakePeer {
    iceGatheringState = "complete";
    connectionState = "new";
    localDescription: any = null;
    remoteDescription: any = null;
    senders: any[] = [];
    closed = false;
    onconnectionstatechange: (() => void) | null = null;
    ontrack: ((e: any) => void) | null = null;
    onicegatheringstatechange: (() => void) | null = null;

    addTrack(track: any) {
        const sender = { track, params: { encodings: [{}] }, getParameters() { return this.params; }, async setParameters(p: any) { this.params = p; } };
        this.senders.push(sender);
        return sender;
    }
    addTransceiver(_kind: string, _init: any) { return {}; }
    getSenders() { return this.senders; }
    async createOffer() { return { type: "offer", sdp: "SDP_OFFER" }; }
    async createAnswer() { return { type: "answer", sdp: "SDP_ANSWER" }; }
    async setLocalDescription(d: any) { this.localDescription = d; }
    async setRemoteDescription(d: any) { this.remoteDescription = d; }
    close() { this.closed = true; }
}

function recordingTransport() {
    const sent: { kind: string; target: string; sdp: string; }[] = [];
    return {
        sent,
        transport: { async send(kind: any, target: string, sdp: string) { sent.push({ kind, target, sdp }); } }
    };
}

const fakeStream = { getTracks: () => [{ kind: "video", id: "t1" }] } as unknown as MediaStream;

describe("BroadcastPeers", () => {
    it("responde uma offer com uma answer para quem mandou", async () => {
        const { sent, transport } = recordingTransport();
        const peers = new BroadcastPeers(fakeStream, transport, {
            budgetMbps: 15,
            createPeer: () => new FakePeer() as any
        });

        await peers.handleOffer("viewer1", "SDP_OFFER");

        assert.equal(sent.length, 1);
        assert.equal(sent[0].kind, "answer");
        assert.equal(sent[0].target, "viewer1");
        assert.equal(sent[0].sdp, "SDP_ANSWER");
    });

    it("conta os viewers conectados", async () => {
        const { transport } = recordingTransport();
        const peers = new BroadcastPeers(fakeStream, transport, {
            budgetMbps: 15,
            createPeer: () => new FakePeer() as any
        });

        await peers.handleOffer("viewer1", "SDP");
        await peers.handleOffer("viewer2", "SDP");
        assert.equal(peers.viewerCount, 2);

        peers.removePeer("viewer1");
        assert.equal(peers.viewerCount, 1);
    });

    it("substitui a conexão quando o mesmo viewer manda outra offer", async () => {
        const { transport } = recordingTransport();
        const created: FakePeer[] = [];
        const peers = new BroadcastPeers(fakeStream, transport, {
            budgetMbps: 15,
            createPeer: () => { const p = new FakePeer(); created.push(p); return p as any; }
        });

        await peers.handleOffer("viewer1", "SDP");
        await peers.handleOffer("viewer1", "SDP");

        assert.equal(peers.viewerCount, 1);
        assert.equal(created[0].closed, true, "a conexão antiga deve ser fechada");
    });

    it("aplica o bitrate dividido pelo número de viewers", async () => {
        const { transport } = recordingTransport();
        const created: FakePeer[] = [];
        const peers = new BroadcastPeers(fakeStream, transport, {
            budgetMbps: 15,
            createPeer: () => { const p = new FakePeer(); created.push(p); return p as any; }
        });

        await peers.handleOffer("viewer1", "SDP");
        // 1 viewer: 15 Mbps cai no teto de 8 Mbps
        assert.equal(created[0].senders[0].params.encodings[0].maxBitrate, 8_000_000);

        await peers.handleOffer("viewer2", "SDP");
        await peers.handleOffer("viewer3", "SDP");
        // 3 viewers: 5 Mbps cada, reaplicado em todos
        assert.equal(created[0].senders[0].params.encodings[0].maxBitrate, 5_000_000);
        assert.equal(created[2].senders[0].params.encodings[0].maxBitrate, 5_000_000);
    });

    it("avisa quando a contagem muda", async () => {
        const { transport } = recordingTransport();
        const counts: number[] = [];
        const peers = new BroadcastPeers(fakeStream, transport, {
            budgetMbps: 15,
            createPeer: () => new FakePeer() as any
        });
        peers.onCountChange = n => counts.push(n);

        await peers.handleOffer("viewer1", "SDP");
        peers.removePeer("viewer1");

        assert.deepEqual(counts, [1, 0]);
    });

    it("fecha todas as conexões em closeAll", async () => {
        const { transport } = recordingTransport();
        const created: FakePeer[] = [];
        const peers = new BroadcastPeers(fakeStream, transport, {
            budgetMbps: 15,
            createPeer: () => { const p = new FakePeer(); created.push(p); return p as any; }
        });

        await peers.handleOffer("viewer1", "SDP");
        await peers.handleOffer("viewer2", "SDP");
        peers.closeAll();

        assert.equal(peers.viewerCount, 0);
        assert.ok(created.every(p => p.closed));
    });
});

describe("ViewerPeer", () => {
    it("manda uma offer para o broadcaster em start", async () => {
        const { sent, transport } = recordingTransport();
        const viewer = new ViewerPeer(transport, "broadcaster1", {
            createPeer: () => new FakePeer() as any
        });

        await viewer.start();

        assert.equal(sent.length, 1);
        assert.equal(sent[0].kind, "offer");
        assert.equal(sent[0].target, "broadcaster1");
        assert.equal(sent[0].sdp, "SDP_OFFER");
    });

    it("aplica a answer recebida", async () => {
        const { transport } = recordingTransport();
        let created: FakePeer;
        const viewer = new ViewerPeer(transport, "broadcaster1", {
            createPeer: () => { created = new FakePeer(); return created as any; }
        });

        await viewer.start();
        await viewer.handleAnswer("SDP_ANSWER");

        assert.equal(created!.remoteDescription.sdp, "SDP_ANSWER");
    });

    it("entrega o stream recebido via ontrack", async () => {
        const { transport } = recordingTransport();
        let created: FakePeer;
        const viewer = new ViewerPeer(transport, "broadcaster1", {
            createPeer: () => { created = new FakePeer(); return created as any; }
        });

        const received: MediaStream[] = [];
        viewer.onStream = s => received.push(s);

        await viewer.start();
        created!.ontrack!({ streams: [fakeStream] });

        assert.deepEqual(received, [fakeStream]);
    });

    it("avisa quando a conexão falha", async () => {
        const { transport } = recordingTransport();
        let created: FakePeer;
        const viewer = new ViewerPeer(transport, "broadcaster1", {
            createPeer: () => { created = new FakePeer(); return created as any; }
        });

        const failures: string[] = [];
        viewer.onFailed = r => failures.push(r);

        await viewer.start();
        created!.connectionState = "failed";
        created!.onconnectionstatechange!();

        assert.equal(failures.length, 1);
    });
});
```

- [ ] **Step 2: Rodar para confirmar que falha**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/peers.test.ts`
Expected: FAIL — `Cannot find module './peers'`

- [ ] **Step 3: Implementar `peers.ts`**

```ts
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import { computePerPeerBitrate } from "./bitrate";
import type { HandshakeKind } from "./codec";
import { ICE_GATHER_TIMEOUT_MS, ICE_SERVERS, PEER_CONNECT_TIMEOUT_MS } from "./constants";

export interface PeerTransport {
    send(kind: HandshakeKind, targetUserId: string, sdp: string): Promise<void>;
}

export type PeerFactory = (config: RTCConfiguration) => RTCPeerConnection;

const defaultFactory: PeerFactory = config => new RTCPeerConnection(config);

const PEER_CONFIG: RTCConfiguration = { iceServers: ICE_SERVERS };

/**
 * Espera o ICE gathering terminar, com teto de tempo.
 * Sinalização por chat não comporta trickle ICE — é uma mensagem por lado,
 * então o SDP precisa sair já com todos os candidatos dentro.
 */
export function waitForIceGathering(
    pc: RTCPeerConnection,
    timeoutMs = ICE_GATHER_TIMEOUT_MS
): Promise<void> {
    if (pc.iceGatheringState === "complete") return Promise.resolve();

    return new Promise(resolve => {
        const finish = () => {
            clearTimeout(timer);
            pc.onicegatheringstatechange = null;
            resolve();
        };

        const timer = setTimeout(finish, timeoutMs);
        pc.onicegatheringstatechange = () => {
            if (pc.iceGatheringState === "complete") finish();
        };
    });
}

/** Lado emissor: uma conexão por viewer, todas com a mesma track. */
export class BroadcastPeers {
    private readonly peers = new Map<string, RTCPeerConnection>();
    private readonly createPeer: PeerFactory;
    private readonly budgetMbps: number;

    onCountChange?: (count: number) => void;

    constructor(
        private readonly stream: MediaStream,
        private readonly transport: PeerTransport,
        opts: { budgetMbps: number; createPeer?: PeerFactory; }
    ) {
        this.budgetMbps = opts.budgetMbps;
        this.createPeer = opts.createPeer ?? defaultFactory;
    }

    get viewerCount(): number {
        return this.peers.size;
    }

    async handleOffer(fromUserId: string, sdp: string): Promise<void> {
        // Uma offer repetida significa que o viewer reconectou: descarta a antiga.
        this.peers.get(fromUserId)?.close();

        const pc = this.createPeer(PEER_CONFIG);
        this.peers.set(fromUserId, pc);

        for (const track of this.stream.getTracks()) {
            pc.addTrack(track, this.stream);
        }

        pc.onconnectionstatechange = () => {
            if (pc.connectionState === "failed" || pc.connectionState === "closed") {
                this.removePeer(fromUserId);
            }
        };

        await pc.setRemoteDescription({ type: "offer", sdp } as RTCSessionDescriptionInit);
        const answer = await pc.createAnswer();
        await pc.setLocalDescription(answer);
        await waitForIceGathering(pc);

        this.applyBitrate();
        this.onCountChange?.(this.peers.size);

        await this.transport.send("answer", fromUserId, pc.localDescription?.sdp ?? answer.sdp!);
    }

    removePeer(userId: string): void {
        const pc = this.peers.get(userId);
        if (!pc) return;

        pc.close();
        this.peers.delete(userId);
        this.applyBitrate();
        this.onCountChange?.(this.peers.size);
    }

    closeAll(): void {
        for (const pc of this.peers.values()) pc.close();
        this.peers.clear();
        this.onCountChange?.(0);
    }

    /** Redistribui o orçamento de upload entre todos os viewers atuais. */
    private applyBitrate(): void {
        const maxBitrate = computePerPeerBitrate(this.budgetMbps, this.peers.size);

        for (const pc of this.peers.values()) {
            for (const sender of pc.getSenders()) {
                if (!sender.track) continue;

                const params = sender.getParameters();
                if (!params.encodings?.length) params.encodings = [{}];
                params.encodings[0].maxBitrate = maxBitrate;
                params.degradationPreference = "maintain-framerate";

                sender.setParameters(params).catch(err =>
                    console.warn("[P2PShare] não deu para aplicar o bitrate", err));
            }
        }
    }
}

/** Lado receptor: uma conexão só, recebendo do broadcaster. */
export class ViewerPeer {
    private pc: RTCPeerConnection | null = null;
    private connectTimer: ReturnType<typeof setTimeout> | null = null;

    onStream?: (stream: MediaStream) => void;
    onFailed?: (reason: string) => void;

    constructor(
        private readonly transport: PeerTransport,
        private readonly broadcasterId: string,
        private readonly opts: { createPeer?: PeerFactory; } = {}
    ) { }

    async start(): Promise<void> {
        const pc = (this.opts.createPeer ?? defaultFactory)(PEER_CONFIG);
        this.pc = pc;

        // recvonly: o viewer não manda mídia nenhuma.
        pc.addTransceiver("video", { direction: "recvonly" });
        pc.addTransceiver("audio", { direction: "recvonly" });

        pc.ontrack = event => {
            const stream = event.streams[0];
            if (stream) {
                this.clearTimer();
                this.onStream?.(stream);
            }
        };

        pc.onconnectionstatechange = () => {
            if (pc.connectionState === "connected") this.clearTimer();
            if (pc.connectionState === "failed") {
                this.fail("a conexão P2P falhou — provável NAT simétrico (CGNAT). Sem TURN não tem como conectar");
            }
        };

        this.connectTimer = setTimeout(
            () => this.fail("tempo esgotado esperando a conexão — provável NAT simétrico (CGNAT)"),
            PEER_CONNECT_TIMEOUT_MS
        );

        const offer = await pc.createOffer();
        await pc.setLocalDescription(offer);
        await waitForIceGathering(pc);

        await this.transport.send("offer", this.broadcasterId, pc.localDescription?.sdp ?? offer.sdp!);
    }

    async handleAnswer(sdp: string): Promise<void> {
        if (!this.pc) return;
        await this.pc.setRemoteDescription({ type: "answer", sdp } as RTCSessionDescriptionInit);
    }

    close(): void {
        this.clearTimer();
        this.pc?.close();
        this.pc = null;
    }

    private clearTimer(): void {
        if (this.connectTimer) {
            clearTimeout(this.connectTimer);
            this.connectTimer = null;
        }
    }

    private fail(reason: string): void {
        this.clearTimer();
        this.onFailed?.(reason);
    }
}
```

- [ ] **Step 4: Rodar os testes**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/peers.test.ts`
Expected: PASS — 10 testes

- [ ] **Step 5: Rodar a suíte inteira**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/codec.test.ts src/userplugins/p2pShare/bitrate.test.ts src/userplugins/p2pShare/capture.test.ts src/userplugins/p2pShare/peers.test.ts`
Expected: PASS — 38 testes

- [ ] **Step 6: Commit**

```bash
cd src/userplugins/p2pShare
git add peers.ts peers.test.ts
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: ciclo de vida das conexões WebRTC"
```

---

### Task 8: `signaling.ts` — beacon e roteamento de handshake

Traduz mensagem do Discord em evento de domínio, e vice-versa.

**Files:**
- Create: `signaling.ts`
- Test: `signaling.test.ts`

**Interfaces:**
- Consumes: `codec.ts` (`embedPayload`, `extractPayload`, `formatHandshakeName`, `parseHandshakeName`, `HandshakeKind`), `constants.ts` (`PROTOCOL_VERSION`, `PLUGIN_URL`, `HANDSHAKE_TTL_MS`), `discord/api.ts`, `discord/events.ts`
- Produces:
  - `interface BeaconPayload { v: number; s: string; }`
  - `interface Beacon { messageId: string; channelId: string; sessionId: string; broadcasterId: string; broadcasterName: string; }`
  - `parseBeacon(message: DiscordMessage): Beacon | null`
  - `beaconContent(sessionId: string, username: string): string`
  - `postBeacon(channelId: string, sessionId: string, username: string): Promise<string>`
  - `sendHandshake(channelId: string, sessionId: string, kind: HandshakeKind, targetUserId: string, sdp: string): Promise<void>`
  - `interface HandshakeEvent { sessionId: string; kind: HandshakeKind; fromUserId: string; sdp: string; }`
  - `observeSignals(handlers: { onBeacon?: (b: Beacon) => void; onBeaconGone?: (channelId: string, messageId: string) => void; onHandshake?: (e: HandshakeEvent) => void; }): () => void`

O corpo de uma mensagem de handshake é o marcador invisível com `{ v, s, k }` — assim o plugin identifica e esconde a mensagem sem precisar baixar o anexo.

- [ ] **Step 1: Escrever os testes que falham**

Create `signaling.test.ts`. Só as funções puras são testadas aqui; `postBeacon` / `sendHandshake` / `observeSignals` dependem do webpack do Discord e são cobertos no teste manual da Task 11.

```ts
import assert from "node:assert/strict";
import { describe, it } from "node:test";

import { beaconContent, parseBeacon } from "./signaling";

function message(overrides: any = {}) {
    return {
        id: "msg1",
        channel_id: "chan1",
        content: beaconContent("abc12345", "Andrew"),
        author: { id: "user1", username: "Andrew" },
        attachments: [],
        ...overrides
    };
}

describe("beaconContent", () => {
    it("mostra o nome de quem transmite", () => {
        assert.ok(beaconContent("abc12345", "Andrew").includes("Andrew"));
    });

    it("inclui o link de instalação", () => {
        assert.match(beaconContent("abc12345", "Andrew"), /https?:\/\//);
    });
});

describe("parseBeacon", () => {
    it("lê um beacon válido", () => {
        const beacon = parseBeacon(message() as any);
        assert.deepEqual(beacon, {
            messageId: "msg1",
            channelId: "chan1",
            sessionId: "abc12345",
            broadcasterId: "user1",
            broadcasterName: "Andrew"
        });
    });

    it("ignora mensagem comum", () => {
        assert.equal(parseBeacon(message({ content: "oi pessoal" }) as any), null);
    });

    it("ignora payload de versão desconhecida", () => {
        const wrongVersion = beaconContent("abc12345", "Andrew").replace(/.*/s, c => c);
        // constrói manualmente um payload v: 99
        const { embedPayload } = require("./codec");
        const content = embedPayload("qualquer", { v: 99, s: "abc12345" });
        assert.equal(parseBeacon(message({ content }) as any), null);
        assert.ok(wrongVersion);
    });

    it("ignora payload sem sessionId", () => {
        const { embedPayload } = require("./codec");
        const content = embedPayload("qualquer", { v: 1 });
        assert.equal(parseBeacon(message({ content }) as any), null);
    });
});
```

- [ ] **Step 2: Rodar para confirmar que falha**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/signaling.test.ts`
Expected: FAIL — `Cannot find module './signaling'`

- [ ] **Step 3: Implementar `signaling.ts`**

```ts
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import {
    embedPayload,
    extractPayload,
    formatHandshakeName,
    type HandshakeKind,
    parseHandshakeName
} from "./codec";
import { HANDSHAKE_TTL_MS, PLUGIN_URL, PROTOCOL_VERSION } from "./constants";
import {
    type DiscordMessage,
    deleteMessage,
    fetchAttachmentText,
    getCurrentUserId,
    sendMessage,
    uploadTextAttachment
} from "./discord/api";
import { onMessageCreate, onMessageDelete } from "./discord/events";

interface BeaconPayload { v: number; s: string; }
interface HandshakeMarker { v: number; s: string; k: HandshakeKind; }

export interface Beacon {
    messageId: string;
    channelId: string;
    sessionId: string;
    broadcasterId: string;
    broadcasterName: string;
}

export interface HandshakeEvent {
    sessionId: string;
    kind: HandshakeKind;
    fromUserId: string;
    sdp: string;
}

/** Texto do beacon: legível para quem não tem o plugin, com o sessionId invisível. */
export function beaconContent(sessionId: string, username: string): string {
    const visible =
        `🔴 **${username}** está transmitindo a tela via P2P.\n` +
        `Instale o plugin para assistir: ${PLUGIN_URL}`;

    return embedPayload(visible, { v: PROTOCOL_VERSION, s: sessionId } satisfies BeaconPayload);
}

export function parseBeacon(message: DiscordMessage): Beacon | null {
    const payload = extractPayload<BeaconPayload>(message.content);
    if (!payload || payload.v !== PROTOCOL_VERSION || typeof payload.s !== "string") return null;
    // Um handshake também carrega payload invisível, mas com o campo `k`.
    if ("k" in payload) return null;

    return {
        messageId: message.id,
        channelId: message.channel_id,
        sessionId: payload.s,
        broadcasterId: message.author.id,
        broadcasterName: message.author.username
    };
}

export function postBeacon(channelId: string, sessionId: string, username: string): Promise<string> {
    return sendMessage(channelId, beaconContent(sessionId, username));
}

export function removeBeacon(channelId: string, messageId: string): Promise<void> {
    return deleteMessage(channelId, messageId);
}

/**
 * Posta um offer/answer como anexo .txt. O corpo carrega só o marcador
 * invisível, para o plugin reconhecer a mensagem sem baixar o anexo.
 */
export async function sendHandshake(
    channelId: string,
    sessionId: string,
    kind: HandshakeKind,
    targetUserId: string,
    sdp: string
): Promise<void> {
    const filename = formatHandshakeName({ sessionId, kind, targetUserId });
    const body = JSON.stringify({ v: PROTOCOL_VERSION, type: kind, sdp });
    const marker = embedPayload("", { v: PROTOCOL_VERSION, s: sessionId, k: kind } satisfies HandshakeMarker);

    await uploadTextAttachment(channelId, filename, body, marker);
}

/**
 * Observa o chat e traduz mensagens em eventos de sinalização.
 * Handshakes endereçados a mim são baixados; o resto é ignorado sem custo.
 * Handshakes que eu mesmo mandei são auto-deletados depois do TTL.
 */
export function observeSignals(handlers: {
    onBeacon?: (beacon: Beacon) => void;
    onBeaconGone?: (channelId: string, messageId: string) => void;
    onHandshake?: (event: HandshakeEvent) => void;
}): () => void {
    const myId = getCurrentUserId();

    const unsubCreate = onMessageCreate(message => {
        const beacon = parseBeacon(message);
        if (beacon) {
            handlers.onBeacon?.(beacon);
            return;
        }

        for (const attachment of message.attachments ?? []) {
            const name = parseHandshakeName(attachment.filename);
            if (!name) continue;

            if (message.author.id === myId) {
                // É meu: some com ele depois que o outro lado teve tempo de baixar.
                setTimeout(() => {
                    deleteMessage(message.channel_id, message.id).catch(() => { });
                }, HANDSHAKE_TTL_MS);
                continue;
            }

            if (name.targetUserId !== myId) continue;

            fetchAttachmentText(attachment.url)
                .then(text => {
                    const body = JSON.parse(text);
                    if (body.v !== PROTOCOL_VERSION || typeof body.sdp !== "string") return;

                    handlers.onHandshake?.({
                        sessionId: name.sessionId,
                        kind: name.kind,
                        fromUserId: message.author.id,
                        sdp: body.sdp
                    });
                })
                .catch(err => console.warn("[P2PShare] handshake ilegível", err));
        }
    });

    const unsubDelete = onMessageDelete((channelId, messageId) => {
        handlers.onBeaconGone?.(channelId, messageId);
    });

    return () => {
        unsubCreate();
        unsubDelete();
    };
}
```

- [ ] **Step 4: Rodar os testes**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/signaling.test.ts`
Expected: PASS — 6 testes

Se o teste falhar por causa do `require` dentro de um módulo ESM, troque as duas linhas `const { embedPayload } = require("./codec");` por um import no topo do arquivo de teste.

- [ ] **Step 5: Verificar tipos**

Run: `pnpm exec tsc --noEmit`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
cd src/userplugins/p2pShare
git add signaling.ts signaling.test.ts
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: beacon e roteamento de handshake"
```

---

### Task 9: `broadcast.ts` — orquestração do lado emissor

**Files:**
- Create: `broadcast.ts`

**Interfaces:**
- Consumes: `capture.ts`, `peers.ts`, `signaling.ts`, `discord/api.ts`, `codec.ts` (`newSessionId`), `index.tsx` (`settings`)
- Produces:
  - `startBroadcast(): Promise<void>`
  - `stopBroadcast(): Promise<void>`
  - `isBroadcasting(): boolean`
  - `onBroadcastStateChange(listener: (state: { active: boolean; viewers: number; }) => void): () => void`

- [ ] **Step 1: Escrever `broadcast.ts`**

```ts
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import { Toasts } from "@webpack/common";

import { captureScreen, CaptureError } from "./capture";
import { newSessionId } from "./codec";
import { getCurrentUserId, getVoiceChannelId } from "./discord/api";
import { settings } from "./index";
import { BroadcastPeers, type PeerTransport } from "./peers";
import { observeSignals, postBeacon, removeBeacon, sendHandshake } from "./signaling";
import { UserStore } from "@webpack/common";

interface Session {
    sessionId: string;
    channelId: string;
    beaconId: string;
    stream: MediaStream;
    peers: BroadcastPeers;
    unsubscribe: () => void;
}

let session: Session | null = null;
const listeners = new Set<(state: { active: boolean; viewers: number; }) => void>();

function notify(): void {
    const state = { active: session !== null, viewers: session?.peers.viewerCount ?? 0 };
    for (const listener of listeners) listener(state);
}

export function onBroadcastStateChange(
    listener: (state: { active: boolean; viewers: number; }) => void
): () => void {
    listeners.add(listener);
    return () => listeners.delete(listener);
}

export function isBroadcasting(): boolean {
    return session !== null;
}

function toast(message: string, type: number): void {
    Toasts.show({ message, type, id: Toasts.genId() });
}

export async function startBroadcast(): Promise<void> {
    if (session) {
        toast("Você já está transmitindo", Toasts.Type.MESSAGE);
        return;
    }

    const channelId = getVoiceChannelId();
    if (!channelId) {
        toast("Entre num canal de voz primeiro", Toasts.Type.FAILURE);
        return;
    }

    let stream: MediaStream;
    try {
        stream = await captureScreen();
    } catch (err) {
        const message = err instanceof CaptureError
            ? err.message
            : `falha inesperada na captura: ${(err as Error).message}`;
        toast(message, Toasts.Type.FAILURE);
        return;
    }

    const sessionId = newSessionId();
    const transport: PeerTransport = {
        send: (kind, targetUserId, sdp) =>
            sendHandshake(channelId, sessionId, kind, targetUserId, sdp)
    };

    const peers = new BroadcastPeers(stream, transport, {
        budgetMbps: settings.store.uploadBudgetMbps
    });
    peers.onCountChange = notify;

    // Se o usuário parar a captura pelo diálogo nativo do Chromium, encerra tudo.
    stream.getVideoTracks()[0]?.addEventListener("ended", () => { void stopBroadcast(); });

    const myId = getCurrentUserId();
    const unsubscribe = observeSignals({
        onHandshake: event => {
            if (event.sessionId !== sessionId || event.kind !== "offer") return;
            peers.handleOffer(event.fromUserId, event.sdp)
                .catch(err => console.error("[P2PShare] falha ao responder offer", err));
        }
    });

    let beaconId: string;
    try {
        beaconId = await postBeacon(channelId, sessionId, UserStore.getCurrentUser().username);
    } catch (err) {
        unsubscribe();
        stream.getTracks().forEach(t => t.stop());
        toast(`não deu para anunciar a transmissão: ${(err as Error).message}`, Toasts.Type.FAILURE);
        return;
    }

    session = { sessionId, channelId, beaconId, stream, peers, unsubscribe };
    void myId;
    notify();
    toast("Transmitindo via P2P", Toasts.Type.SUCCESS);
}

export async function stopBroadcast(): Promise<void> {
    const current = session;
    if (!current) return;

    session = null;

    current.unsubscribe();
    current.peers.closeAll();
    current.stream.getTracks().forEach(track => track.stop());

    try {
        await removeBeacon(current.channelId, current.beaconId);
    } catch (err) {
        console.warn("[P2PShare] não deu para apagar o beacon", err);
    }

    notify();
    toast("Transmissão encerrada", Toasts.Type.MESSAGE);
}
```

- [ ] **Step 2: Verificar tipos e build**

Run: `pnpm exec tsc --noEmit && pnpm build`
Expected: PASS nos dois

- [ ] **Step 3: Commit**

```bash
cd src/userplugins/p2pShare
git add broadcast.ts
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: orquestração do lado emissor"
```

---

### Task 10: `watch.ts` e `ui/ViewerOverlay.tsx` — lado receptor

**Files:**
- Create: `watch.ts`
- Create: `ui/ViewerOverlay.tsx`
- Create: `ui/overlay.css`

**Interfaces:**
- Consumes: `peers.ts` (`ViewerPeer`), `signaling.ts` (`observeSignals`, `sendHandshake`, `Beacon`), `index.tsx` (`settings`)
- Produces:
  - `startWatching(beacon: Beacon): Promise<void>`
  - `stopWatching(sessionId: string): void`
  - `initWatcher(): () => void` — assina o chat, devolve a função de limpeza
  - `getActiveBeacons(): Beacon[]`
  - `onBeaconsChange(listener: (beacons: Beacon[]) => void): () => void`
  - `ui/ViewerOverlay.tsx`: `mountOverlay(sessionId: string, stream: MediaStream, onClose: () => void): void`, `unmountOverlay(sessionId: string): void`

- [ ] **Step 1: Escrever `ui/overlay.css`**

```css
.p2ps-overlay {
    position: fixed;
    z-index: 3000;
    background: var(--background-tertiary, #202225);
    border-radius: 8px;
    box-shadow: var(--elevation-high, 0 8px 16px rgba(0, 0, 0, .24));
    overflow: hidden;
    display: flex;
    flex-direction: column;
    resize: both;
    min-width: 240px;
    min-height: 160px;
}

.p2ps-overlay-bar {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 6px 8px;
    cursor: move;
    background: var(--background-secondary-alt, #292b2f);
    color: var(--header-primary, #fff);
    font-size: 13px;
    user-select: none;
}

.p2ps-overlay-title {
    flex: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.p2ps-overlay-bar button {
    background: none;
    border: none;
    color: var(--interactive-normal, #b9bbbe);
    cursor: pointer;
    padding: 2px 6px;
    font-size: 13px;
}

.p2ps-overlay-bar button:hover {
    color: var(--interactive-hover, #fff);
}

.p2ps-overlay video {
    flex: 1;
    width: 100%;
    background: #000;
    object-fit: contain;
}
```

- [ ] **Step 2: Escrever `ui/ViewerOverlay.tsx`**

```tsx
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import "./overlay.css";

import { createRoot, type Root } from "@webpack/common";
import { React, useEffect, useRef, useState } from "@webpack/common";

import { settings } from "../index";

interface OverlayProps {
    stream: MediaStream;
    title: string;
    onClose: () => void;
}

function ViewerOverlay({ stream, title, onClose }: OverlayProps) {
    const videoRef = useRef<HTMLVideoElement>(null);
    const [pos, setPos] = useState({ x: settings.store.overlayX, y: settings.store.overlayY });
    const [width, setWidth] = useState(settings.store.overlayWidth);
    const drag = useRef<{ dx: number; dy: number; } | null>(null);

    useEffect(() => {
        if (videoRef.current) videoRef.current.srcObject = stream;
    }, [stream]);

    useEffect(() => {
        const onMove = (e: MouseEvent) => {
            if (!drag.current) return;
            setPos({ x: e.clientX - drag.current.dx, y: e.clientY - drag.current.dy });
        };
        const onUp = () => {
            if (!drag.current) return;
            drag.current = null;
            settings.store.overlayX = pos.x;
            settings.store.overlayY = pos.y;
        };

        window.addEventListener("mousemove", onMove);
        window.addEventListener("mouseup", onUp);
        return () => {
            window.removeEventListener("mousemove", onMove);
            window.removeEventListener("mouseup", onUp);
        };
    }, [pos]);

    const startDrag = (e: React.MouseEvent) => {
        drag.current = { dx: e.clientX - pos.x, dy: e.clientY - pos.y };
    };

    const goFullscreen = () => { void videoRef.current?.requestFullscreen(); };

    return (
        <div
            className="p2ps-overlay"
            style={{ left: pos.x, top: pos.y, width }}
            onMouseUp={e => {
                const next = (e.currentTarget as HTMLElement).offsetWidth;
                if (next !== width) {
                    setWidth(next);
                    settings.store.overlayWidth = next;
                }
            }}
        >
            <div className="p2ps-overlay-bar" onMouseDown={startDrag}>
                <span className="p2ps-overlay-title">🔴 {title}</span>
                <button onClick={goFullscreen} title="Tela cheia">⛶</button>
                <button onClick={onClose} title="Fechar">✕</button>
            </div>
            <video ref={videoRef} autoPlay playsInline controls />
        </div>
    );
}

const mounted = new Map<string, { root: Root; container: HTMLElement; }>();

export function mountOverlay(
    sessionId: string,
    stream: MediaStream,
    title: string,
    onClose: () => void
): void {
    unmountOverlay(sessionId);

    const container = document.createElement("div");
    container.id = `p2ps-overlay-${sessionId}`;
    document.body.appendChild(container);

    const root = createRoot(container);
    root.render(<ViewerOverlay stream={stream} title={title} onClose={onClose} />);

    mounted.set(sessionId, { root, container });
}

export function unmountOverlay(sessionId: string): void {
    const entry = mounted.get(sessionId);
    if (!entry) return;

    entry.root.unmount();
    entry.container.remove();
    mounted.delete(sessionId);
}

export function unmountAllOverlays(): void {
    for (const sessionId of [...mounted.keys()]) unmountOverlay(sessionId);
}
```

Se `createRoot` não estiver exportado de `@webpack/common`, use `import { createRoot } from "@webpack/common/react"` ou o helper `ReactDOM` que o Vencord expõe — confirme em `src/webpack/common/react.ts`.

- [ ] **Step 3: Escrever `watch.ts`**

```ts
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import { Toasts } from "@webpack/common";

import { getCurrentUserId } from "./discord/api";
import { ViewerPeer, type PeerTransport } from "./peers";
import { type Beacon, observeSignals, sendHandshake } from "./signaling";
import { mountOverlay, unmountAllOverlays, unmountOverlay } from "./ui/ViewerOverlay";

/** Beacons vivos, por messageId. */
const beacons = new Map<string, Beacon>();
/** Sessões que estou assistindo, por sessionId. */
const watching = new Map<string, ViewerPeer>();

const beaconListeners = new Set<(beacons: Beacon[]) => void>();

function notifyBeacons(): void {
    const list = [...beacons.values()];
    for (const listener of beaconListeners) listener(list);
}

export function getActiveBeacons(): Beacon[] {
    return [...beacons.values()];
}

export function onBeaconsChange(listener: (beacons: Beacon[]) => void): () => void {
    beaconListeners.add(listener);
    return () => beaconListeners.delete(listener);
}

function toast(message: string, type: number): void {
    Toasts.show({ message, type, id: Toasts.genId() });
}

export async function startWatching(beacon: Beacon): Promise<void> {
    if (watching.has(beacon.sessionId)) return;

    const transport: PeerTransport = {
        send: (kind, targetUserId, sdp) =>
            sendHandshake(beacon.channelId, beacon.sessionId, kind, targetUserId, sdp)
    };

    const peer = new ViewerPeer(transport, beacon.broadcasterId);
    watching.set(beacon.sessionId, peer);

    peer.onStream = stream => {
        mountOverlay(
            beacon.sessionId,
            stream,
            beacon.broadcasterName,
            () => stopWatching(beacon.sessionId)
        );
    };

    peer.onFailed = reason => {
        toast(reason, Toasts.Type.FAILURE);
        stopWatching(beacon.sessionId);
    };

    try {
        await peer.start();
        toast(`Conectando com ${beacon.broadcasterName}…`, Toasts.Type.MESSAGE);
    } catch (err) {
        toast(`não deu para pedir a transmissão: ${(err as Error).message}`, Toasts.Type.FAILURE);
        stopWatching(beacon.sessionId);
    }
}

export function stopWatching(sessionId: string): void {
    watching.get(sessionId)?.close();
    watching.delete(sessionId);
    unmountOverlay(sessionId);
}

/** Assina o chat para descobrir beacons e receber answers. Devolve a limpeza. */
export function initWatcher(): () => void {
    const myId = getCurrentUserId();

    const unsubscribe = observeSignals({
        onBeacon: beacon => {
            // Meu próprio beacon não me interessa como viewer.
            if (beacon.broadcasterId === myId) return;

            beacons.set(beacon.messageId, beacon);
            notifyBeacons();
        },

        onBeaconGone: (_channelId, messageId) => {
            const beacon = beacons.get(messageId);
            if (!beacon) return;

            beacons.delete(messageId);
            stopWatching(beacon.sessionId);
            notifyBeacons();
        },

        onHandshake: event => {
            if (event.kind !== "answer") return;
            watching.get(event.sessionId)
                ?.handleAnswer(event.sdp)
                .catch(err => console.error("[P2PShare] answer inválida", err));
        }
    });

    return () => {
        unsubscribe();
        for (const sessionId of [...watching.keys()]) stopWatching(sessionId);
        unmountAllOverlays();
        beacons.clear();
        notifyBeacons();
    };
}
```

- [ ] **Step 4: Verificar tipos e build**

Run: `pnpm exec tsc --noEmit && pnpm build`
Expected: PASS nos dois

- [ ] **Step 5: Commit**

```bash
cd src/userplugins/p2pShare
git add watch.ts ui/
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: lado receptor com overlay flutuante"
```

---

### Task 11: Wiring final — comando, botão no beacon e ciclo de vida

**Files:**
- Modify: `index.tsx` (substituir por completo)
- Create: `ui/BeaconAccessory.tsx`

**Interfaces:**
- Consumes: `broadcast.ts`, `watch.ts`, `signaling.ts` (`parseBeacon`)
- Produces: plugin completo com `start()` / `stop()`, comando `/p2pshare`, e botão **Assistir** no beacon

- [ ] **Step 1: Escrever `ui/BeaconAccessory.tsx`**

```tsx
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import { Button } from "@webpack/common";
import type { Message } from "@vencord/discord-types";

import { getCurrentUserId } from "../discord/api";
import { parseBeacon } from "../signaling";
import { startWatching } from "../watch";

/** Renderiza o botão Assistir abaixo de um beacon. Devolve null se não for um. */
export function BeaconAccessory({ message }: { message: Message; }) {
    const beacon = parseBeacon(message as any);
    if (!beacon) return null;
    if (beacon.broadcasterId === getCurrentUserId()) return null;

    return (
        <Button
            size={Button.Sizes.SMALL}
            color={Button.Colors.BRAND}
            onClick={() => { void startWatching(beacon); }}
        >
            Assistir transmissão P2P
        </Button>
    );
}
```

- [ ] **Step 2: Reescrever `index.tsx`**

```tsx
/*
 * Vencord, a Discord client mod
 * Copyright (c) 2026 Vendicated and contributors
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import { addMessageAccessory, removeMessageAccessory } from "@api/MessageAccessories";
import { definePluginSettings } from "@api/Settings";
import { ApplicationCommandInputType, ApplicationCommandOptionType, sendBotMessage } from "@api/Commands";
import definePlugin, { OptionType } from "@utils/types";

import { isBroadcasting, startBroadcast, stopBroadcast } from "./broadcast";
import { DEFAULT_BUDGET_MBPS } from "./constants";
import { BeaconAccessory } from "./ui/BeaconAccessory";
import { initWatcher } from "./watch";

export const settings = definePluginSettings({
    uploadBudgetMbps: {
        type: OptionType.SLIDER,
        description: "Orçamento de upload em Mbps, dividido entre os viewers",
        markers: [5, 10, 15, 25, 50, 100],
        default: DEFAULT_BUDGET_MBPS,
        stickToMarkers: false
    },
    overlayX: { type: OptionType.NUMBER, description: "", default: 80, hidden: true },
    overlayY: { type: OptionType.NUMBER, description: "", default: 80, hidden: true },
    overlayWidth: { type: OptionType.NUMBER, description: "", default: 640, hidden: true }
});

let cleanupWatcher: (() => void) | null = null;

export default definePlugin({
    name: "P2PShare",
    description: "Compartilhamento de tela ponto-a-ponto, sem passar pela infra de vídeo do Discord",
    authors: [{ name: "Andrew", id: 0n }],
    settings,

    commands: [{
        name: "p2pshare",
        description: "Inicia ou encerra a transmissão de tela P2P",
        inputType: ApplicationCommandInputType.BUILT_IN,
        options: [{
            name: "acao",
            description: "iniciar ou parar",
            type: ApplicationCommandOptionType.STRING,
            required: false,
            choices: [
                { name: "iniciar", value: "iniciar", label: "iniciar" },
                { name: "parar", value: "parar", label: "parar" }
            ]
        }],
        execute: (args, ctx) => {
            const action = args.find(a => a.name === "acao")?.value ?? (isBroadcasting() ? "parar" : "iniciar");

            if (action === "parar") {
                void stopBroadcast();
                sendBotMessage(ctx.channel.id, { content: "Encerrando a transmissão P2P." });
            } else {
                void startBroadcast();
                sendBotMessage(ctx.channel.id, { content: "Iniciando a transmissão P2P…" });
            }
        }
    }],

    start() {
        cleanupWatcher = initWatcher();
        addMessageAccessory("p2pshare", props => <BeaconAccessory message={props.message} />);
    },

    stop() {
        removeMessageAccessory("p2pshare");
        void stopBroadcast();
        cleanupWatcher?.();
        cleanupWatcher = null;
    }
});
```

Confirme os nomes reais de `addMessageAccessory` / `removeMessageAccessory` em `src/api/MessageAccessories.tsx` antes de rodar — a assinatura mudou entre versões do Vencord. Se a API atual pedir um objeto de opções em vez de dois argumentos, ajuste a chamada.

- [ ] **Step 3: Verificar tipos e build**

Run: `pnpm exec tsc --noEmit && pnpm build`
Expected: PASS nos dois

- [ ] **Step 4: Rodar toda a suíte de testes**

Run: `pnpm exec tsx --test src/userplugins/p2pShare/codec.test.ts src/userplugins/p2pShare/bitrate.test.ts src/userplugins/p2pShare/capture.test.ts src/userplugins/p2pShare/peers.test.ts src/userplugins/p2pShare/signaling.test.ts`
Expected: PASS — 44 testes

- [ ] **Step 5: Commit**

```bash
cd src/userplugins/p2pShare
git add index.tsx ui/BeaconAccessory.tsx
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "feat: comando, botão Assistir e ciclo de vida do plugin"
```

---

### Task 12: Verificação manual ponta a ponta

WebRTC real e patches do Discord não dão para automatizar. Este é o único teste que prova que o plugin funciona.

**Files:** nenhum (só verificação; correções viram commits próprios)

- [ ] **Step 1: Instalar em duas máquinas**

Run: `pnpm build && pnpm inject`

Repita em uma segunda máquina (ou peça para alguém instalar). As duas precisam do plugin ativo. Reinicie o Discord nas duas.

- [ ] **Step 2: Testar o caminho feliz**

1. As duas contas entram no mesmo canal de voz.
2. Na máquina A: `/p2pshare` → escolher a tela.
3. Confirmar que a mensagem 🔴 aparece no chat do canal.
4. Na máquina B: clicar em **Assistir transmissão P2P**.
5. Confirmar que o overlay abre e mostra a tela de A.
6. Arrastar e redimensionar o overlay; confirmar que a posição persiste ao fechar e reabrir.

Registre o que falhar. Cada correção vira um commit próprio.

- [ ] **Step 3: Testar o encerramento**

1. Na máquina A: `/p2pshare parar`.
2. Confirmar que a mensagem 🔴 some do chat.
3. Confirmar que o overlay em B fecha sozinho.

- [ ] **Step 4: Testar o auto-delete do handshake**

Depois de uma conexão, esperar 20s e confirmar que as mensagens com anexo `p2p.*.txt` sumiram do chat, deixando só o beacon.

- [ ] **Step 5: Testar viewer entrando no meio**

Com a transmissão já rolando, um terceiro cliente entra no canal, rola o chat até o beacon e clica em Assistir. Deve conectar normalmente.

- [ ] **Step 6: Testar a queda do bitrate**

Com 3 viewers, abrir `about:webrtc-internals` (ou o console) na máquina A e confirmar que o `maxBitrate` por sender caiu para 5 Mbps com o orçamento padrão de 15.

- [ ] **Step 7: Testar a saída abrupta**

Fechar o Discord da máquina A no braço. Em B, o overlay deve fechar em até 30s por timeout de conexão, com o toast de erro.

- [ ] **Step 8: Anotar os resultados no README**

Acrescente ao `README.md` uma seção "Testado em" com as versões do Discord e do Vencord, e as limitações encontradas.

- [ ] **Step 9: Commit**

```bash
cd src/userplugins/p2pShare
git add README.md
git -c user.name="Andrew" -c user.email="andrew.mautone@gmail.com" commit -m "docs: resultados da verificação manual"
```

---

## Self-Review

**Cobertura do spec:**

| Requisito do spec | Task |
|---|---|
| Topologia mesh, uma PC por viewer | 7 |
| STUN público | 1, 7 |
| Beacon legível + payload zero-width | 2, 8 |
| Botão Assistir no lugar do beacon | 11 |
| Handshake sempre em anexo `.txt` | 5, 8 |
| Roteamento pelo nome do arquivo | 2, 8 |
| Conteúdo JSON sem compressão | 8 |
| Auto-delete do handshake em 20s | 8 |
| ICE não-trickle com teto de 4s | 7 |
| Máquina de estados do broadcaster | 9 |
| Máquina de estados do viewer | 10 |
| Banda adaptativa por viewer | 3, 7 |
| Cadeia de fallback da captura | 6 |
| Envio de anexo via CloudUpload | 5 |
| Botão no painel de voz | — ver nota |
| Comando `/p2pshare` | 11 |
| Overlay arrastável/redimensionável | 10 |
| Contador de viewers | 9 (estado exposto por `onBroadcastStateChange`) |
| Erro de ICE com mensagem de CGNAT | 7, 10 |
| Erro de captura indisponível | 6, 9 |
| Beacon deletado fecha viewers | 10 |
| Retry no upload do anexo | — ver nota |
| Testes unitários de codec/bitrate/peers | 2, 3, 7 |
| Testes manuais | 12 |

**Notas de escopo deliberadas:**

1. **Botão no painel de voz não tem task.** O spec o lista como caminho primário e o comando slash como fallback. Na prática o patch depende de um regex contra o bundle atual do Discord, que não dá para escrever sem ter o bundle em mãos. O plano entrega o comando slash, que é funcionalmente completo; o botão vira um follow-up depois da Task 12, quando dá para inspecionar o bundle ao vivo. **Isto é uma redução consciente de escopo em relação ao spec** — o plugin fica totalmente utilizável sem ele.

2. **Retry no upload do anexo não tem task.** O spec pede "retry 1x, depois aborta aquele peer". A Task 8 aborta sem retry. Adicionar o retry é uma mudança de três linhas em `sendHandshake` e vale mais fazer depois da Task 12, quando se souber se a falha de upload acontece de verdade.

3. **`observeSignals` é assinado duas vezes** — uma por `broadcast.ts`, outra por `watch.ts`. Cada uma filtra o que lhe interessa (`offer` vs `answer`). É desperdício pequeno e mantém os dois lados independentes; unificar seria otimização prematura.

**Consistência de tipos:** `PeerTransport.send(kind, targetUserId, sdp)` tem a mesma assinatura em `peers.ts` (Task 7) e nos dois consumidores (Tasks 9 e 10). `Beacon` tem os mesmos cinco campos em `signaling.ts` (Task 8) e em quem consome (Tasks 10, 11). `HandshakeKind` é definido em `codec.ts` (Task 2) e importado por `peers.ts` e `signaling.ts`.
